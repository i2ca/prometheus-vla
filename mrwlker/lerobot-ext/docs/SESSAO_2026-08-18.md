# Diário da sessão de 18/08/2026 — FastWAM-D de ponta a ponta

Escrito para você poder desligar o PC e retomar sem reconstruir contexto.
**O treino roda na athena, dentro de um `screen`: desligar seu PC não o afeta.**

---

## 1. O que está no ar agora (20:44 de 18/08/2026)

```
athena (hercules@10.9.8.252)
└── screen train0            treino FastWAM-D, GPU 0
    ├── step 267/10000, 1,32 s/step, ETA ~3h33
    ├── eval_loss no step 250: 0,4253
    └── wandb: https://wandb.ai/prometheus-lcad/prometheus_g1/runs/7nx90agt
```

Conferir de qualquer máquina:

```bash
ssh hercules@10.9.8.252
screen -r train0                    # Ctrl+A depois D para sair sem matar
# ou sem entrar no screen:
tail -c 300 /data/train_output/fastwamdepth.log | tr '\r' '\n' | tail -1
grep -E "eval_loss=|novo melhor" /data/train_output/fastwamdepth.log | tail
```

Se o screen disser que está em uso: `screen -r -d train0` (toma a sessão) ou
`screen -x train0` (acompanha junto).

**O servidor de inferência está DESLIGADO.** Para subir de novo:

```bash
screen -dmS infer bash /data/train_output/launch_server_fastwamd.sh 0
```

### Arquivos na athena que importam

| caminho | o que é |
|---|---|
| `/data/train_output/fastwamdepth_white_cup_on_dripper/` | saída da corrida atual |
| `/data/train_output/fastwamd_teste_step1000/` | **o melhor checkpoint da corrida 1** (eval 0,3266), congelado |
| `/data/train_output/fastwamdepth_white_cup_on_dripper_corrida1_ate_step5000/` | corrida 1 inteira, preservada |
| `/data/train_output/*.sh`, `wandb_sidecar.py` | versionados em `lerobot-ext/maquinas/athena/` |
| `/data/.cache/huggingface` | 35 GB de pesos do Wan — não apagar, custa horas para rebaixar |

---

## 2. O que ficou pendente (em ordem de urgência)

**1. O fork do lerobot não foi empurrado.** O repo pai já está no GitHub
apontando para `d52a098b`, que só existe na sua máquina. Quem clonar agora
quebra no `git submodule update`.

```bash
git -C lerobot push origin prometheus-vla/v0.6.1
```

**2. A ablação nunca rodou.** São três corridas do mesmo YAML:

```bash
bash /data/train_output/launch_fastwamd.sh 0 --policy.depth_mode=token --job_name=fastwamdepth_token
bash /data/train_output/launch_fastwamd.sh 0 --policy.depth_mode=off   --job_name=fastwamdepth_off
```

Sem elas não há resposta para a pergunta da pesquisa: a profundidade no latente
ajuda? Cada uma precisa de `output_dir` próprio ou do diretório anterior movido.

**3. A garra.** É o defeito real medido (ver seção 4). Nada foi feito ainda.

---

## 3. O que foi construído hoje

| arquivo | o que faz |
|---|---|
| `init_lerobot_inference_fastwamd_server.py` | servidor na athena: modelo de 6 B, ZMQ, trava de faixa, captura de debug |
| `init_lerobot_inference_fastwamd_client.py` | cliente no seu PC: loop de controle, replay de dataset, rampa de entrada |
| `viz_debug_fastwamd.py` | painel de 4 quadrantes (atenção, profundidade, nuvem, temperatura) |
| `avaliar_episodio_fastwamd.py` | replay de episódio gravado com métricas por grupo de juntas |
| `policies/fastwam_depth/debug_inferencia.py` | captura de atenção e profundidade de uma inferência |
| `maquinas/athena/` | os launchers, com as armadilhas de ambiente documentadas |
| `docs/INFERENCIA_FASTWAMD.md` | o desenho completo |

Correções no que já existia: `set_dataset_feature_metadata` (o `ValueError` de
298), o "só o melhor checkpoint" (duas vezes — ver seção 5), a validação
uniforme, a config do robô com `use_waist_yaw`, o OpenCV headless.

---

## 4. O diagnóstico do modelo (o que os números dizem)

Medido com o checkpoint do **step 1000** da corrida 1, pelo replay de episódios:

| | ep 5 (treino) | ep 25 (held-out) |
|---|---|---|
| erro médio geral | 0,0860 rad | 0,1415 rad |
| braço direito | 0,0824 | 0,1134 |
| **mão direita** | **0,2180** | **0,3474** |

**A tarefa é um degrau binário.** As seis juntas da mão direita se movem juntas,
entre 0 e ±1,500 rad. O `thumb_0` não se move (base fixa). "Pegar a xícara" é um
evento liga/desliga.

**O modelo fecha a mão na hora certa e não segura.** No episódio de TREINO ele
fecha no quadro 195 e vai abrindo sozinho até o zero, enquanto o gravado fica
cravado em −1,5. No held-out fecha tarde, oscila e abre 80 quadros cedo demais.
No robô isso é: pega o copo e deixa cair.

Isso acontece **até nos dados que ele treinou** — não é falta de generalização, é
a garra sendo aprendida errado. Um degrau regredido com objetivo contínuo, com 22
exemplos de "fecha" e 22 de "abre", vira uma rampa que decai para a média.

**Overfitting confirmado** na corrida 1: eval_loss 0,3977 (step 500) → **0,3266
(step 1000, o mínimo)** → 0,3319 → 0,3550 (3000) → 0,3919 (4500), enquanto a loss
de treino caía para 0,065.

**14 das 29 dimensões são constantes** (braço e mão esquerdos, desligados por
defeito de hardware). Qualquer métrica média sobre as 29 é diluída por elas.

### As quatro linhas de melhoria, em ordem de custo-benefício

1. **Tratar a garra como binária** — limiar com histerese nas seis juntas da mão.
   Roda sobre o checkpoint que já existe, sem retreinar. O FastWAM tem
   `toggle_action_dimensions` no config para isso (assume ±1, a nossa é 0→±1,5).
2. **Peso na loss** para as 6 juntas da mão, ou cortar o espaço de ação de 29
   para 15 tirando as dimensões mortas.
3. **Mais dados**, e especificamente mais eventos de garra.
4. **Menos parâmetros treináveis** — 1,02 B para 11 mil quadros. LoRA no action
   expert.

---

## 5. Armadilhas encontradas (para não cair de novo)

**OpenCV headless.** O core do lerobot depende de `opencv-python-headless`
(`GUI: NONE`): nenhuma janela deste repo abre. Corrigido no `install.sh` (passo
8b) e o painel agora falha com instrução em vez de ficar mudo.

**Segfault com zmq + torch na athena.** O libstdc++ do sistema não tem
`GLIBCXX_3.4.29`; o libzmq carrega o do sistema primeiro e as extensões do
diffusers estouram depois, no import do lerobot, sem mensagem. Conserto:
`LD_PRELOAD` do libstdc++ do conda (está nos launchers).

**`python` não existe em shell não interativo.** `screen -dmS` e `ssh host cmd`
não passam pelo `.bashrc`. Os launchers usam caminho absoluto.

**`init_logging` limpa os handlers do logging** dentro do `train()`. O capturador
de `eval_loss` do "só o melhor" era varrido antes do primeiro eval, e o
checkpoint guardado virava "o último". Corrigido embrulhando o `init_logging`.

**Classe local não pickla.** A primeira versão da amostragem uniforme definia a
classe dentro da função; o DataLoader de validação em *spawn* pickla o dataset e
morreu no primeiro eval com `Can't get local object`. Corrigido devolvendo o
`Subset` original com os índices trocados. **Teste que faltava: round-trip de
pickle.**

**`max_eval_samples` pega os N primeiros quadros**, ou seja, só o início do
primeiro episódio held-out. O `eval_loss` melhoraria enquanto a garra piora.
Corrigido com espaçamento uniforme sobre todo o held-out.

**Attention sink.** O mapa de atenção cru é dominado pelas duas primeiras colunas
em todo quadro (0,48 e 0,27 contra 0,03). O painel mostra `(atual − base) / base`
— a razão, não a subtração, porque o sumidouro flutua.

**Cadência.** 32 ações por inferência e ~1,5 s por inferência: a 30 Hz o buffer
zera entre chunks e o robô anda em solavancos. Use `--fps=15`.

**O simulador não é o robô.** Com render do MuJoCo o modelo produzia ação da
ordem de 10⁵ rad (travada em 1,65 pela trava de faixa); com quadro real do
dataset, zero travadas. O `TRAVADAS n` no painel é o instrumento: enquanto não
for zero, não dá para julgar o comportamento.

---

## 6. Como retomar

```bash
ssh hercules@10.9.8.252
screen -r train0
grep -E "eval_loss=|novo melhor" /data/train_output/fastwamdepth.log
```

O melhor checkpoint vai estar em
`/data/train_output/fastwamdepth_white_cup_on_dripper/checkpoints/best`,
reescrito a cada melhora do eval. Para testá-lo, copie para um diretório
congelado (o servidor não deve ler o `best` do treino, que é trocado por rename
atômico embaixo dele) e suba o servidor:

```bash
cp -a .../checkpoints/best/pretrained_model /data/train_output/fastwamd_teste_stepN/
screen -dmS infer bash /data/train_output/launch_server_fastwamd.sh 0 \
    /data/train_output/fastwamd_teste_stepN/pretrained_model
```

E no seu PC:

```bash
cd ~/DEV/prometheus-vla/mrwlker/lerobot-ext && python init_lerobot_inference_fastwamd_client.py \
    --server=10.9.8.252 --sim --replay=meu_dataset/white_cup_on_dripper_2026-08-11:25 \
    --chunk=32 --lead=24 --fps=15 --v-debug --debug
```

O histórico bruto desta conversa fica em
`~/.claude/projects/-home-miguel-DEV-prometheus-vla/*.jsonl` — é local à sua
máquina e sobrevive ao desligamento, mas não vai para o git.
