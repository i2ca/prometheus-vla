robot:
  type: unitree_g1_dex3
  control_mode: "upper_body"

teleop:
  type: xr_g1_arm
  zmq: true
  input_mode: "hand"
  display_mode: "ego"
  ee_type: "dex3"

dataset:
  repo_id: Mrwlker/pick_up_the_cup2
  root: "meu_dataset/pick_up_the_cup2"
  push_to_hub: true
  single_task: "pick up the cup"
  # Na 0.6.1 o codec vive dentro de `rgb_encoder`. Profundidade não passa por
  # aqui: usa o `depth_encoder` (HEVC gray12le sem perda).
  rgb_encoder:
    vcodec: "h264"

play_sounds: false